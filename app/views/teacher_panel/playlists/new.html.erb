<% content_for :title, "New Playlist" %>

<div class="container-fluid mt-4">

  <div class="mb-4">
    <h2 class="fw-bold">
      ➕ Add Playlist
    </h2>

    <p class="text-muted mb-0">
      Course:
      <strong>
        <%= @course.Course_name %>
      </strong>
    </p>
  </div>

  <%= render "form" %>

</div>